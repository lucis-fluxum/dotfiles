# lucis-fluxum's dotfiles

`setup.sh` creates symlinks. Existing dotfiles are backed up to `~/.dotfiles_old`.

Added a new Vim plugin? Make sure it gets registered here with `git submodule add [url] vim/bundle/[name]`.
