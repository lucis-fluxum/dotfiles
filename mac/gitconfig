[user]
    name = Luc Street
    email = lucis-fluxum@users.noreply.github.com
    signingkey = B50141371B40B526

[core]
    editor = vi --clean

[diff]
    algorithm = histogram

[credential]
    helper = osxkeychain

[pull]
    rebase = false

[pager]
    diff = delta
    show = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    features = side-by-side
    plus-style = syntax "#003800"
